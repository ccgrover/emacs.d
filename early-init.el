;;; early-init.el --- (Early) init! -*- lexical-binding: t -*-
;;; Commentary:

;;  Early init stuff.

;;; Code:

(setenv "LSP_USE_PLISTS" "true")

;;; _
(provide 'early-init)
;;; early-init.el ends here
